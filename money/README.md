# exchangeMOney — Простая демонстрация обмена валют (статичный сайт)

💱 Currency Converter — Простой конвертер валют

Этот проект — лёгкое и удобное веб-приложение для конвертации валют.
Все данные берутся из бесплатного сервиса:
https://open.er-api.com/v6/latest
API не требует ключа, работает бесплатно и стабильно.

🚀 Функционал
✔ Конвертация валют

Пересчёт любых валют в реальном времени.

✔ Автоматическая загрузка списка валют

Список валют подгружается прямо из API.

✔ Сохранение последнего значения

Поля сохраняют текущие значения, чтобы можно было быстро менять направление конвертации.

✔ Простое и минималистичное UI

Две строки:

amount + from currency

result + to currency

🧠 Как работает приложение

script.js делает запрос в API:

const data = await fetch(API_URL).then(r => r.json());


Заполняет <select id="from"> и <select id="to"> всеми валютами.

При нажатии Convert:

определяется курс между выбранными валютами

производится вычисление:

result = amount * (rate_to / rate_from)


Результат появляется в поле #result.

Внизу выводится текст текущего курса:

1 USD = 89.34 KGS

▶ Как запустить проект

Скачай файлы

Открой index.html в браузере

Готово — приложение работает без сервера или деплоя

🌐 Демо возможностей

Поддерживает все валюты из API (100+ валют)

Работает мгновенно

Полностью без ключей и ограничений